indexname,indexdef
one_active_session_per_user,CREATE UNIQUE INDEX one_active_session_per_user ON public.sessions USING btree (user_id) WHERE (status = 'active'::text)
sessions_pkey,CREATE UNIQUE INDEX sessions_pkey ON public.sessions USING btree (id)